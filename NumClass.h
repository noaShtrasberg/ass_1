#ifndef NUMCLASS_H_

/*will return if a number is Armstrongnumber*/
int isArmstrong(int);
/*will return if a number is palindrome*/
int isPalindrome(int);
/*will return if a number is prime*/
int isPrime(int);
/*Strong number is special number whose sum of the factorial of digits is equal to the original number*/
int isStrong(int);

#endif